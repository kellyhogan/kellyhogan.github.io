import React from "react"

import "./Contact.css"

export default ({id}) => (
  <div id={id}>
  <h1>This is the contact section</h1>
  <p>This is the contact section</p>
  <p>This is the contact section</p>
  </div>
);